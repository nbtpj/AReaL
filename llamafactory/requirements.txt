# Single-machine SFT for qwen3vl8b-thinking variants.
# One-step install (NO git clone needed):
#
#     pip install -r requirements.txt
#
# Verified target: Python 3.12, CUDA 12.8, 8x GPU node (AReaL cluster).
#
# IMPORTANT - version drift vs the cluster's previous 0.9.5.dev0 editable install:
# pinning LlamaFactory to PyPI 0.9.4 triggers these forced downgrades, baked into
# 0.9.4's own pyproject.toml version caps:
#
#   transformers : cluster had 5.0.0.dev0  ->  capped at <= 4.57.1
#   peft         : cluster had 0.18.1      ->  capped at <= 0.17.1
#   deepspeed    : cluster had 0.19.0      ->  capped at <= 0.16.9 (via [deepspeed] extra)
#
# Re-run both `train_v{2,4}.sh` after install to re-verify SFT stability before
# trusting longer training runs.

# PyTorch: PyPI default for torch 2.8.0 ships the CUDA 12.8 build (matches cluster).
# For a different CUDA, override:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Required by `report_to: tensorboard` in both YAMLs - NOT in LlamaFactory's deps.
tensorboard==2.16.2

# LlamaFactory + monitoring extras (nltk, jieba, rouge-chinese via [metrics])
# + deepspeed (<=0.16.9, via [deepspeed]).
# Both `metrics` and `deepspeed` are real optional-dependencies in v0.9.4's pyproject.toml.
llamafactory[metrics,deepspeed]==0.9.4
